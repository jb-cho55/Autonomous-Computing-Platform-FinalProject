# ============================================================
#  ERIKA Enterprise RTOS / 빌드 생성물
#  makefile 의 `make config` (코드 생성) 와 `make build` (컴파일) 로
#  conf.oil 로부터 자동 재생성되므로 저장소에 커밋하지 않습니다.
# ============================================================
**/erika/
**/out/

# 컴파일 산출물 (혹시 다른 위치에 생성될 경우 대비)
*.o
*.a
*.elf
*.hex
*.lss
*.lst
*.map
*.sym
*.d

# ============================================================
#  대용량 발표 자료 (GitHub 단일 파일 100MB 제한 초과 — 약 123MB)
#  필요 시 외부 스토리지 링크로 공유하세요.
# ============================================================
docs/김경재_조정빈.pptx

# ============================================================
#  OS / 에디터 생성 파일
# ============================================================
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~

# Claude Code 로컬 설정 (개인 환경)
.claude/settings.local.json
